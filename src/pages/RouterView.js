/** @jsx jsx */
import { jsx } from "@emotion/core";

const LoginView = () => {
  return (
    <main
      css={{
        height: "100%",
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      Test short
    </main>
  );
};

export default LoginView;
